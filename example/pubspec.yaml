name: scan_it_example
description: A new Flutter module project.
publish_to: none

version: 1.0.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  scan_it:
    path: ../
  logger: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: br.com.luanemanuel.scan_it_example
    iosBundleIdentifier: br.com.luanemanuel.scan_it_example